---
name: backend-review
description: LUNOTE 백엔드(NestJS API) 코드 리뷰 절차. 백엔드/API 코드 변경을 리뷰할 때, 또는 사용자가 백엔드 코드 리뷰를 요청할 때 사용한다.
---

# 백엔드 코드 리뷰

실결제가 발생하는 상용 서비스의 백엔드를 리뷰한다. 아래 절차와 기준을 따른다.

## 절차

1. **먼저 변경 목적과 영향 범위를 파악한다.**
   diff만 보지 말고 관련 이슈/PR 본문, 호출부, 연관 모듈까지 확인해서
   "이 변경이 왜 필요하고 어디까지 영향을 주는가"를 리뷰 서두에 요약한다.

2. **정확성, 보안, 동시성, 트랜잭션, 성능, 예외 처리를 검토한다.**
   - 정확성: 상태 머신 전이 규칙, 경계값, null/undefined 처리
   - 보안: 인증/인가 누락, 입력 검증, 시크릿/개인정보 노출(로그 포함), 인젝션
   - 동시성: 동시 요청 레이스, 멱등성(특히 웹훅 이벤트ID UNIQUE), 중복 처리
   - 트랜잭션: 다중 쓰기의 원자성, 부분 실패 시 정합성, 트랜잭션 경계
   - 성능: N+1 쿼리, 인덱스 유무, 페이지네이션 누락, 불필요한 직렬 I/O
   - 예외 처리: 실패 경로의 사용자 응답, 재시도 가능성, 에러 삼킴 여부

3. **기존 테스트가 보장하지 못하는 엣지 케이스를 찾는다.**
   apps/api/test의 e2e 스펙을 확인하고, 이번 변경으로 새로 생기는
   미커버 경로(동시 호출, 실패 주입, 권한 경계)를 명시한다.

## 마이그레이션 검토 (diff에 schema.prisma 또는 prisma/migrations/ 변경이 있으면 필수)

운영 DB 기준으로 가정하고 검토한다: 테이블에 데이터가 있고 트래픽이 흐르며,
API는 롤링 배포라 마이그레이션 적용 후에도 구버전 파드가 수 분간 함께 돈다.
"현재 규모가 작으니 괜찮다"는 판단은 하지 않는다 — 규모를 모르면 크다고 가정.

- **파괴적 변경**: rename이 DROP+ADD로 생성됐는지(Prisma 기본 동작 — RENAME
  COLUMN으로 수동 수정), 컬럼/테이블 DROP·타입 변경의 데이터 유실 여부.
  NOT NULL 컬럼 추가는 DEFAULT 지정 또는 nullable→백필→NOT NULL 3단계인지.
- **롤링 배포 호환(expand/contract)**: 적용 시점에 구버전 코드가 제거·변경된
  컬럼을 읽거나 쓰지 않는지. 컬럼 제거는 참조 코드가 모두 배포된 다음
  릴리스로 분리하는 게 원칙.
- **락**: 큰 테이블의 인덱스 생성은 CREATE INDEX CONCURRENTLY 필요
  (트랜잭션 밖 실행이라 Prisma 마이그레이션에서 분리 실행 방안까지 확인).
- **제약 사전 검증**: UNIQUE/FK/CHECK 추가 전, 기존 데이터가 제약을 위반하지
  않는지 운영 DB에서 확인하는 절차가 계획에 있는지. 위반 시 정리 절차 포함.
- **백필·롤백**: 새 필수 컬럼의 기존 행 값을 채우는 계획, 실패 시 되돌릴
  방법이 있는지.

## 결과 보고 기준

- **반드시 수정할 문제와 개선 제안을 구분한다.** (🔴 필수 수정 / 🟡 개선 제안)
- **문제를 지적할 때 발생 조건과 수정 방향을 함께 제시한다.**
  "X가 위험하다"가 아니라 "조건 A에서 B가 발생한다 → C로 수정" 형태로.
- **근거 없는 칭찬과 단순한 취향 차이는 제외한다.**
  스타일 지적은 프로젝트 컨벤션(CLAUDE.md, 기존 패턴) 위반일 때만.
