# LUNOTE Mobile

Expo(React Native) 앱. 실행 방법은 저장소 루트의 CLAUDE.md 참조.

```bash
npm run ios                  # 개발 빌드 + 시뮬레이터 (네이티브 모듈 변경 시)
npx expo start --dev-client  # Metro만 (평소)
npm run web                  # 웹 미리보기
```
